<Window x:Class="SqlMonitorUI.LiveQueryEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Live Monitoring Query" 
        Height="600" 
        Width="900"
        WindowStyle="ToolWindow" 
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock x:Name="HeaderText" 
                       Text="Edit Query: " 
                       FontSize="18" 
                       FontWeight="SemiBold" 
                       Foreground="#333333"/>
            <TextBlock x:Name="DescriptionText"
                       Text=""
                       FontSize="13"
                       Foreground="#666666"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Settings Row -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="4"
                Padding="15"
                Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <CheckBox x:Name="EnabledCheckBox" 
                          Content="Enabled" 
                          VerticalAlignment="Center"
                          Margin="0,0,30,0"/>
                
                <TextBlock Text="Timeout (sec):" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="TimeoutTextBox" Width="60" Margin="0,0,30,0"/>
                
                <TextBlock Text="Refresh Every N Ticks:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="RefreshTicksTextBox" Width="60" Margin="0,0,30,0"/>
                
                <TextBlock Text="(1 tick = 1 second typically)" 
                           Foreground="#888888" 
                           VerticalAlignment="Center"
                           FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <!-- SQL Editor -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F0F0F0" Padding="10">
                    <TextBlock Text="SQL Query" FontWeight="SemiBold"/>
                </Border>

                <TextBox Grid.Row="1"
                         x:Name="SqlTextBox"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="13"
                         Padding="10"
                         BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Reset to Default"
                    Click="ResetButton_Click"
                    Padding="15,10"
                    Margin="0,0,10,0"
                    Background="#D83B01"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="13"
                    Cursor="Hand"/>
            
            <Button Content="💾 Save"
                    Click="SaveButton_Click"
                    Padding="20,10"
                    Margin="0,0,10,0"
                    Background="#107C10"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Cursor="Hand"/>
            
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Padding="15,10"
                    Background="#666666"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="13"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
